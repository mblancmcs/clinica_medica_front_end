import { EncurtarNomePipe } from './encurtar-nome.pipe';

describe('EncurtarNomePipe', () => {
  it('create an instance', () => {
    const pipe = new EncurtarNomePipe();
    expect(pipe).toBeTruthy();
  });
});
